linters:
  disable-all: true
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

    - gofumpt
    - gci
    - unconvert

linters-settings:
  gci:
    sections:
      - standard
      - default
      - prefix(github.com/aaronjheng/kafka-cli)
    custom-order: true
